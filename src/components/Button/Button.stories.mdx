import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import Button from "./Button";

<Meta title="Components/Button" component={Button} />

# Button

Create some default description here.

<ArgsTable of={Button} />

## Button example

<Canvas>
  <Story name="Button Example" args={{ children: "Texto do botão" }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

## Another example

<Canvas>
  <Story name="Another Example" args={{ children: "Another example" }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>
